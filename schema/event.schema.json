{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/today-in-history/hub/event.schema.json",
  "title": "HistoryEvent",
  "description": "One historical event for Today in History storage and API responses.",
  "type": "object",
  "additionalProperties": false,
  "required": ["date", "category", "title", "description"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Surrogate key when stored in a database (optional in static files)."
    },
    "date": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "Calendar date of the event (ISO 8601 date, YYYY-MM-DD)."
    },
    "category": {
      "type": "string",
      "minLength": 1,
      "description": "Grouping label for UI sections (e.g. 政治・行政)."
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "subtitle": {
      "type": "string",
      "description": "Secondary line (tagline, location, era)."
    },
    "summary": {
      "type": "string",
      "description": "Short overview (one or two sentences)."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Full narrative or long explanation."
    },
    "images": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": [],
      "description": "Image URLs or storage paths."
    },
    "year": {
      "type": "string",
      "description": "Legacy or ingest-helper label (e.g. 2004年). Prefer deriving year from date."
    },
    "locale_calendar_label": {
      "type": "string",
      "description": "Optional localized calendar label from ingest (e.g. 4月20日)."
    },
    "source_run_id": {
      "type": "string",
      "description": "Batch or run identifier for provenance and idempotent re-ingestion."
    }
  }
}
